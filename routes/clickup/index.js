const comments = require('./clickupComments')
const folders = require('./clickupFolders')
const lists = require('./clickupLists')
const spaces = require('./clickupSpaces')
const tasks = require('./clickupTasks')

module.exports = {
    comments, folders, lists, spaces, tasks
}