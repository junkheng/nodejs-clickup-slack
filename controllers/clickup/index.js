const folder = require('./FolderController')
const comment = require('./CommentController')
const list = require('./ListController')
const space = require('./SpaceController')
const task = require('./TaskController')

module.exports = {
    folder, comment, list, space, task
}